<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 定义间隔-->
    <!--  <stroke
        android:width="8dp"
        android:color="@color/black" />-->
    <!-- todo 设置透明底色-->
    <solid android:color="@android:color/primary_text_dark_nodisable" />
    <corners android:radius="@dimen/radius_btn_10" />
    <padding
        />
</shape>